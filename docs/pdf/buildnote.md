# Build note — SmallFloats.pdf (built 2026-08-18)

Generated by docs/pdf/buildpdf.sh (pipeline: docs/pdf/howto.md). 205 pages, Letter.

## Toolchain
MiKTeX-XeTeX 4.18 (MiKTeX 26.5); Latexmk, John Collins, 15 June 2025. Version 4.87;
Pygments Pygments version 2.21.0, (c) 2006-present by Georg Brandl, MatthÃ¤us Chajdas and contributors.; DejaVu Sans / DejaVu Sans Mono;
Documenter LaTeX writer (platform="none" via docs/make_latex.jl);
validation: pdftoppm/pdfplumber/pypdf/qpdf.

## Reorganizations relative to docs/make.jl
- parts flattened: False (8 one-chapter parts)
- duplicate labels uniquified: 23 (none were link targets — asserted)
- regrouping candidates (editorial, howto.md Stage 2 — not auto-applied):
  section 'User Guide' (6 subsections; avg 10 est lines; short spans), section 'Rounding and Saturation' (8 subsections; avg 11 est lines; short spans), section 'Defined, Stochastic, and Approximate Results' (7 subsections; avg 10 est lines; short spans), section 'Control Rounding and Overflow' (9 subsections; avg 11 est lines; short spans), section 'Performance Model' (7 subsections; avg 10 est lines; short spans), section 'Encoding and Decoding' (8 subsections; avg 8 est lines; short spans), section 'Projection Engine' (8 subsections; avg 9 est lines; short spans), section 'Function Tables and Array Kernels' (7 subsections; avg 10 est lines; short spans), section 'Exact Block Reductions' (7 subsections; avg 13 est lines; short spans)

## Special handling
- 280 code blocks (longest 42 lines, all wrapped unbreakable);
  33 whitespace-adjacent chains glued; 138 colon-introduced blocks glued
- 170 docstrings wrapped (breakable glue between consecutive boxes — rule 8)
- 79 tabulary tables: prose columns R->L, kept whole at footnotesize
- 64 table-bearing headings kept with their tables (rule 3, \needspace);
  12 oversized-extent tables glued to their introducing paragraph
- \allowbreak after 1190 escaped underscores (prose/texttt only)
- largest TOC chapter block: 3 section entries (rule 9 headroom)
- Unicode fallbacks: ⟺

## Verification
Gate 4: zero Overfull vbox, zero missing characters, no multiply-defined labels,
TOC populated; worst residual Overfull hbox 37.17235pt.
Gate 5 battery (validate.py): qpdf clean, text extracts on every page (no U+FFFD),
bookmarks present, no near-blank pages, margin ink respected, no code-band
page-boundary crossings, TOC block integrity (rule 9) clean.

## Reproducing
    julia docs/builddocs.jl         # HTML + PDF (set DOCS_PDF=skip to omit)
    bash docs/pdf/buildpdf.sh       # PDF only (docs env must be instantiated)
Content changes regenerate from docs/src — never edit the LaTeX by hand.
