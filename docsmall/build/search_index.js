var documenterSearchIndex = {"docs":
[{"category":"section","location":"reference.html#Quick-Reference","page":"Quick Reference","text":"Names and signatures in brief. For semantics, see the preceding chapters; for exhaustive tables, the full manual's Reference section.","title":"Quick Reference"},{"category":"section","location":"reference.html#Values-in-and-out","page":"Quick Reference","text":"Binary8p4se(1.6)          # Real → project to nearest datum\nBinary8p4se(0x45)         # Unsigned → CODE POINT (validated)\nConvert(T, ρ, x)          # explicit projection, numeric for every integer\ndecode(x)                 # the exact datum, on the format's carrier\ncodepoint(x)              # the stored identity\nT(codepoint(x)) === x     # the round-trip law","title":"Values in and out"},{"category":"section","location":"reference.html#Format-queries","page":"Quick Reference","text":"Pure functions of the type parameters; each takes a type or a value, and folds to a constant either way.\n\nbitwidth(T)  precision(T)  issigned(T)  isextended(T)\nexpbias(T)   expbitwidth(T)  trailingsigbits(T)\nMaxFiniteOf(T)  MinFiniteOf(T)  MinPositiveOf(T)\nMinNormalOf(T)  MaxSubnormalOf(T)\nformat(K, P, SGN, EXT)    # programmatic route to any of the 504\n\nDraft-named twins exist throughout: BitwidthOf, PrecisionOf, ExponentBiasOf, ….","title":"Format queries"},{"category":"section","location":"reference.html#Projection-specifications","page":"Quick Reference","text":"ProjSpec(rounding, saturation); every deterministic pairing is predefined as R<mode>_S<mode>:\n\n SatNone SatFinite SatPropagate\nNearestTiesToEven RNE_SN RNE_SF RNE_SP\nNearestTiesToAway RNA_SN RNA_SF RNA_SP\nTowardPositive RTP_SN RTP_SF RTP_SP\nTowardNegative RTN_SN RTN_SF RTN_SP\nTowardZero RTZ_SN RTZ_SF RTZ_SP\nToOdd RTO_SN RTO_SF RTO_SP\n\nStochastic constructors: RSA_SN(N), RSB_SF(N), RSC_SP(N), … — families A/B/C crossed with the saturation modes, N random bits (default 8). Pass rng for a stream or R ∈ 0:2^N−1 for one exact draw. RNE_SN is the initial session default.","title":"Projection specifications"},{"category":"section","location":"reference.html#Operations","page":"Quick Reference","text":"52 draft operations — 30 unary, 18 binary, 3 ternary, plus Convert — every one spelled Op(fr, ρ, operands…) with a same-format convenience Op(x…), array methods, and Block/Scaled variants. Families: sign and arithmetic (Abs … Divide, fused FMA/FAA), roots and exp/log, trig, hyperbolic, π-scaled trig, Softplus/Hypot/ArcTan2, extrema and Clamp, comparisons and TotalOrder, format queries, NextGreaterThan / NextLessThan. Base spellings (+, exp, sqrt, min, nextfloat, …) map one-to-one onto same-format calls under the session default.","title":"Operations"},{"category":"section","location":"reference.html#Session-defaults","page":"Quick Reference","text":"Read with DefaultX(), set with DefaultX!(v):\n\nDefaultType        DefaultReturnType\nDefaultRoundingMode  DefaultSaturationMode  DefaultProjection\n\nThe projection default and its two component defaults stay coherent no matter which setter ran last. All five are process-global; name T and ρ explicitly in anything that must be reproducible.","title":"Session defaults"},{"category":"section","location":"reference.html#Display","page":"Quick Reference","text":"set_show_style!(s) / get_show_style(), or per-stream IOContext(io, :binary_show_style => s), with s ∈ (:value, :codepoint, :datum, :typed) — e.g. 1.625 · 0x45 · 1.625 ⇆ 0x45 · Binary8p4se(1.625 ⇆ 0x45). Presentation only; no result ever depends on it.","title":"Display"},{"category":"section","location":"reference.html#Blocks,-storage,-conformance","page":"Quick Reference","text":"Block(scale, elems...)                       # scale × element per lane\nConvertToBlockMaxAbsFinite(FS, FE, ρs, ρe, elems)\nBlockReduceAdd(T, ρ, b);  BlockDotProduct(T, ρ, a, b)\nPackedVector(v)                              # true bit-width storage\nmeasure_kappa(f, op, T, argTs, ρ)            # exhaustive κ measurement\nregister_approx!(name, op, T, argTs, ρ, f; κ)\nconformance_dict();  draft_revision()","title":"Blocks, storage, conformance"},{"category":"section","location":"reference.html#Common-mistakes","page":"Quick Reference","text":"T(2) where T(0x02) was meant, or the reverse. The integer type's signedness selects value versus code point, and both calls succeed.\nVector of the abstract Binary{K,P,SGN,EXT}. The abstract format boxes every element; use the concrete alias.\nA format or spec held in a non-const global. Every call then pays dynamic dispatch (about 1 µs), and benchmarks measure the dispatcher rather than the package.\nReading SatNone as a clamp. Only SatFinite guarantees a finite result; SatNone applies the draft's rows and can produce ±Inf or NaN.\nStaging raw data through the narrow element format before block-quantizing. The clamp then happens before the block scale can absorb it; stage through a wide-range format instead.","title":"Common mistakes"},{"category":"section","location":"values.html#Formats-and-Values","page":"Formats and Values","text":"This chapter defines the format space, shows how a value names a datum, and states the constructor rule that prevents the most common usage error.","title":"Formats and Values"},{"category":"section","location":"values.html#The-format-space","page":"Formats and Values","text":"Every format is an instance of one parametric type, Binary{K,P,SGN,EXT}: bitwidth K ∈ 3:16, precision P (significand bits, implicit bit included), SGN for signed or unsigned, EXT for extended (with ±Inf) or finite. Each legal combination — 504 in total — has a concrete alias whose name encodes the parameters: Binary8p4se is 8-bit, precision 4, signed, extended.\n\nThe bitwidth is a fixed budget, spent entirely:\n\n(Image: Bit-field anatomy of Binary8p4se: one sign bit, four exponent bits, three stored significand bits, with the leading significand bit implicit.)\n\nOne sign bit if signed, E = K − P + 1 − SGN exponent bits, and P − 1 stored significand bits. Raising precision by one bit removes one exponent bit; choosing a format is choosing this trade.\n\nusing SmallFloats exports the 120 aliases at K ≤ 8. The other 384 are available three ways — fully qualified, via using SmallFloats.Formats, or programmatically:\n\njulia> T = format(16, 6, true, true)\nBinary16p6se\n\njulia> codeunit_type(T), SmallFloats.datumcarrier(T)\n(UInt16, Float64)\n\nCode points occupy a UInt8 at K ≤ 8 and a UInt16 above. decode returns the format's datum carrier: Float64 for the 432 formats it holds exactly, Float128 or an exact dyadic carrier for the other 72. Both are representation details; the value model does not change with K.\n\nOne practical caution. The abstract type Binary{8,4,true,true} is a supertype of the concrete Binary8p4se, and it is not a valid array element type: Vector of the abstract format boxes every element and loses all of the performance machinery. Use the concrete alias, or format(...), in element types.","title":"The format space"},{"category":"section","location":"values.html#A-format,-listed-in-full","page":"Formats and Values","text":"Small formats can be inspected completely. Binary4p2se has 16 code points, and this listing is the entire format:\n\njulia> decode.(sort(Binary4p2se.(0x00:0x0f)))\n16-element Vector{Float64}:\n NaN\n -Inf\n  -2.0\n  -1.5\n  -1.0\n  -0.75\n  -0.5\n  -0.25\n   0.0\n   0.25\n   0.5\n   0.75\n   1.0\n   1.5\n   2.0\n  Inf\n\nThe same table for any format, in storage order or total order, is a few lines. No projection is involved — decode is exact and policy-free, so the table depends on the format alone:\n\nfunction codetable(::Type{F}; by::Symbol = :code) where {F<:Binary}\n    U = codeunit_type(F)                        # UInt8 at K ≤ 8, UInt16 above\n    vals = [F(U(c)) for c in 0:(1 << bitwidth(F)) - 1]\n    by === :value && sort!(vals)                # total order: NaN first\n    [(code = codepoint(v), value = decode(v)) for v in vals]\nend\n\ncodetable(Binary4p2se) lists code 0x08 — the sign bit alone — as the single NaN, with negatives mirroring the positives above it; codetable(Binary4p2se; by = :value) reproduces the sorted listing above. The reverse table, value → code, does need a projection: codepoint(Convert(F, ρ, x)), one rounding per entry.\n\nThree P3109 design decisions are visible here. There is exactly one NaN, with no payloads and no quiet/signalling distinction. There is no negative zero; T(-0.0) projects to the single zero. And NaN sorts first, below −Inf: the draft's total order places it at the opposite end from Float64 sorting, which gives sorting and searching deterministic behavior. Finite formats (suffix f) assign the two infinity code points to two additional finite datums.","title":"A format, listed in full"},{"category":"section","location":"values.html#Construction-projects","page":"Formats and Values","text":"julia> x = Binary8p4se(1.6)\nBinary8p4se(1.625 ⇆ 0x45)\n\nThe number 1.6 is not among the format's values. Its nearest neighbors are 1.5, 1.625, and 1.75, spaced 0.125 apart in this binade, so construction rounds to the nearest datum and stores that datum's code point. Constructors from a Real never throw for representable magnitudes; they round.\n\ndecode and codepoint read back the two halves:\n\njulia> decode(x), codepoint(x)\n(1.625, 0x45)\n\ndecode never rounds; it is a lookup from code point to datum. The asymmetry matters: reading is exact, writing projects.","title":"Construction projects"},{"category":"section","location":"values.html#The-code-point-rule","page":"Formats and Values","text":"An Unsigned argument means code point. Every other Real argument means a value to project. The integer type's signedness is the entire distinction:\n\njulia> Binary8p4se(0x02), Binary8p4se(2)\n(Binary8p4se(0.001953125 ⇆ 0x02), Binary8p4se(2.0 ⇆ 0x48))\n\nThe same digit, read two ways, differs by a factor of 1024 here — and neither call is an error, so a wrongly-typed integer produces a valid, different result rather than a failure. Use unsigned integers when you mean stored identity (a byte read from a file, a wire format). Use signed integers or any other Real when you mean a number. When intent must be explicit regardless of the argument's type, Convert(T, ρ, x) always projects numerically.\n\nThe round-trip law holds in one direction: T(codepoint(x)) === x for every valid code point. The other direction fails by design — decode(T(1.6)) returns 1.625, because construction already chose a datum, and nothing downstream can recover the original argument.","title":"The code-point rule"},{"category":"section","location":"values.html#Predicates,-stepping,-classification","page":"Formats and Values","text":"The standard numeric vocabulary applies to every format: isnan, isinf, isfinite, iszero, signbit, issubnormal, along with zero, one, eps, typemin, and typemax, all answering from the format's own grid. Stepping moves to the adjacent code point, and Class reports the draft's classification:\n\njulia> NextGreaterThan(Binary8p4se(1.0))\nBinary8p4se(1.125 ⇆ 0x41)\n\njulia> Class(Binary8p4se(0.01))\nClassPosNormal::FPClass = 0x06\n\nNextGreaterThan and NextLessThan are the draft's Next operations; nextfloat and prevfloat alias them.\n\nOne related distinction: Base's lowercase convert is value-preserving on Unsigned input — convert(Binary8p4se, 0x02) is the value 2.0, not code point 2 — because Julia's promotion machinery, similar, and fill depend on that behavior. The code-point reading belongs to the constructor alone. Prefer T(x) or Convert(T, ρ, x) in your own code and leave bare convert to the places Base itself calls it.","title":"Predicates, stepping, classification"},{"category":"section","location":"values.html#Display","page":"Formats and Values","text":"Four styles render the same value: :value (1.625), :codepoint (0x45), :datum (1.625 ⇆ 0x45), and :typed (Binary8p4se(1.625 ⇆ 0x45)). Select one process-wide with set_show_style! or per-stream with the :binary_show_style IOContext property. Display is presentation only; no kernel or comparison reads it. This guide uses :typed throughout.","title":"Display"},{"category":"section","location":"values.html#Next","page":"Formats and Values","text":"Values exist but nothing has computed with them. Construction from a Real rounded, and this chapter did not say by what rule. The rule is a policy the caller selects, and it is the subject of Computing.","title":"Next"},{"category":"section","location":"worked_example.html#A-Worked-Example","page":"A Worked Example","text":"The preceding chapters in one task: choose a format for real data, quantize, measure what the choice cost, and record what produced the numbers. The pattern transfers directly to model weights, activations, or any signal.","title":"A Worked Example"},{"category":"section","location":"worked_example.html#The-data,-and-an-explicit-policy","page":"A Worked Example","text":"A fixed seed makes the session reproducible; RNE_SF — nearest, ties to even, clamp to finite — is named once and used throughout, so the report stays meaningful even if a session default changes elsewhere:\n\nusing SmallFloats, Random, Statistics\n\nrng = Xoshiro(42)\nw = randn(rng, 50_000) .* 0.25        # a typical trained-weight scale","title":"The data, and an explicit policy"},{"category":"section","location":"worked_example.html#Measure,-with-three-numbers","page":"A Worked Example","text":"One number is not enough evidence for a quantization choice. Aggregate error can look fine while single values are badly wrong, and both can look fine while the tails overflow. Report all three:\n\nfunction quantization_report(::Type{F}, x; projection = RNE_SF) where {F}\n    q = [Convert(F, projection, xi) for xi in x]\n    y = decode.(q)\n    finite = isfinite.(y)\n    (\n        format = formatname(F),\n        rmse = sqrt(mean((x[finite] .- y[finite]).^2)),\n        max_error = maximum(abs.(x[finite] .- y[finite])),\n        nonfinite_rate = count(!isfinite, y) / length(y),\n        boundary_rate = count(v -> abs(v) == decode(MaxFiniteOf(F)), y) / length(y),\n    )\nend\n\nquantization_report(Binary8p4se, w)\n\n(format = :Binary8p4se, rmse = 0.0066, max_error = 0.0622,\n nonfinite_rate = 0.0, boundary_rate = 0.0)\n\nAverage damage is small, the worst single value is off by ten times the average, and nothing overflowed — for this sample, range was never the binding constraint.","title":"Measure, with three numbers"},{"category":"section","location":"worked_example.html#Compare-candidates-under-one-policy","page":"A Worked Example","text":"Same sample, same projection, four formats — never change the data and the policy in the same experiment:\n\nfor F in (Binary8p5se, Binary8p4se, Binary8p3se, Binary8p2se)\n    r = quantization_report(F, w)\n    println(rpad(String(r.format), 12), \" rmse=\", round(r.rmse; sigdigits=3),\n            \"  max=\", round(r.max_error; sigdigits=3),\n            \"  nonfinite=\", r.nonfinite_rate, \"  boundary=\", r.boundary_rate)\nend\n\nBinary8p5se  rmse=0.00356  max=0.0256  nonfinite=0.0  boundary=0.0\nBinary8p4se  rmse=0.00662  max=0.0622  nonfinite=0.0  boundary=0.0\nBinary8p3se  rmse=0.0132   max=0.0881  nonfinite=0.0  boundary=0.0\nBinary8p2se  rmse=0.0261   max=0.162   nonfinite=0.0  boundary=0.0\n\nRMSE roughly doubles per significand bit given up, exactly as the datum spacing does. Every format holds this data without overflow, so the range the lower-precision formats offer goes unused and Binary8p5se wins outright. On wider-spread data the last two columns stop reading zero and the ranking changes — which is what they are there to catch.","title":"Compare candidates under one policy"},{"category":"section","location":"worked_example.html#Inspect-the-worst-misses","page":"A Worked Example","text":"q = [Convert(Binary8p4se, RNE_SF, x) for x in w]\nerr = abs.(w .- decode.(q))\nworst = partialsortperm(err, 1:3; rev = true)\n[(w[i], decode(q[i]), err[i]) for i in worst]\n\n(-1.06279, -1.125, 0.06221)\n(-1.06092, -1.0,   0.06092)\n(-1.06967, -1.125, 0.05533)\n\nThe worst errors cluster just past 1.0, where Binary8p4se's spacing widens to 0.125 at the binade boundary — a structural property of the format, not an unlucky sample. Knowing where the errors live says whether a different format would help or the data needs rescaling. If failures cluster by row or channel scale instead, use a block scale (see Data at Scale).","title":"Inspect the worst misses"},{"category":"section","location":"worked_example.html#Record-what-produced-the-result","page":"A Worked Example","text":"d = conformance_dict()          # formats, operations, specializations, approximations\nrev = draft_revision()          # \"IEEE P3109/D1, uploaded 2026-07-17\"\n\nSerialize d with any JSON or TOML writer and store it, the draft revision, the chosen format, the projection name, and the seed beside the quantized artifact. A later reader can then reproduce the numbers or explain a difference without guessing what the session looked like.","title":"Record what produced the result"},{"category":"section","location":"worked_example.html#Next","page":"A Worked Example","text":"Assurance covers the record-keeping used above in full: the provenance test, measured approximation, and how the defined path itself is verified.","title":"Next"},{"category":"section","location":"troubleshooting.html#Troubleshooting","page":"Troubleshooting","text":"Symptom-first. Each entry names what you see, why, and the fix.\n\nT(0x02) and T(2) give different values, and it looks like a bug. An Unsigned argument is a code point; every other integer is a value to project. Both calls are valid, so nothing warns. Fix: T(Int(c)) when you mean the number, T(c::Unsigned) when you mean the code point, or Convert(T, ρ, x) when intent must be explicit. See Formats and Values.\n\nAn array of Binary values is slow, or every element is boxed. The element type is the abstract parametric format, which is not isbits. Fix: use a concrete alias as the element type (Vector{Binary8p4se}), or obtain one from format(...) when the parameters are runtime values.\n\nA benchmark reports about 1 µs per call for a warm scalar operation. The format or spec was read from a non-const global, so every call pays dynamic dispatch; the benchmark measures the dispatcher. Fix: pass formats as type parameters or const bindings, and verify zero warm-path allocation before trusting a number.\n\nOverflow produced Inf where a clamp was expected (or the reverse). SatNone is not a clamp; it applies the draft's domain-, sign-, and direction-dependent rows, which can yield ±Inf, an extremal finite value, or NaN. Only SatFinite guarantees a finite result. See Computing.\n\nMixing two Binary formats throws a promotion error. Deliberate: no automatic rule can soundly pick a containing format. Fix: Convert(T, ρ, x) to the format you want before combining.\n\nConstructing from a Rational throws. Also deliberate: it would require a hidden second rounding. Fix: convert to a supported carrier first (T(Float64(r))) and own that rounding.\n\nA stochastic pipeline gives different results each run. Stochastic projections draw random bits. Fix: pass a seeded rng for a reproducible stream, or an explicit R in 0:2^N−1 for bit-exact single projections in tests. Thread one rng through every call that must agree; constructing a second generator mid-pipeline desynchronizes the streams.\n\nThe first array call is much slower than every later one. Deterministic unary/binary calls may build a cached result table on first use; you measured the build. Fix: warm the exact specialization first; benchmark cold and warm separately. table_bytes() inspects the cache, empty_tables!() resets it.\n\nFloat16/BFloat16 data comes through at half or double its value. Bits were reinterpreted across an exponent-bias difference of one. Fix: always Convert, never reinterpret, across the IEEE boundary. See Data at Scale.\n\nBlock quantization did not help data with wide per-row scales. The raw values were staged through the narrow element format first, so SatFinite clamped them before the block scale could absorb the range. Fix: stage through a wide-range format, then ConvertToBlockMaxAbsFinite.\n\nsort puts NaN first, unlike Float64 sorting. Not a bug: the draft's total order places the single NaN below −Inf. Sort with rev=true if you need it last.\n\nDivision by zero returned NaN, not Inf. P3109 defines Divide(x, 0) as NaN for every x, and Recip(±Inf) as 0. Check divisors explicitly if an algorithm relies on IEEE signed-infinity quotients.","title":"Troubleshooting"},{"category":"section","location":"data.html#Data-at-Scale","page":"Data at Scale","text":"One rule governs this chapter: acceleration may change how work is scheduled, never which code point is returned. Arrays, cached tables, fused block reductions, and packed storage are four schedules for the same exact-then-project computation.","title":"Data at Scale"},{"category":"section","location":"data.html#Arrays-and-tables","page":"Data at Scale","text":"Every operation has array methods with the scalar argument shape, operands replaced by arrays. Each element is exactly the scalar answer:\n\njulia> A = Binary8p4se.([-1.0, 0.5, 2.0]);\n\njulia> Exp(Binary8p4se, RNE_SN, A)\n3-element Vector{Binary8p4se}:\n Binary8p4se(0.375 ⇆ 0x34)\n Binary8p4se(1.625 ⇆ 0x45)\n Binary8p4se(7.5 ⇆ 0x57)\n\nUnder a deterministic projection, a unary or binary operation is a finite function on code points, so the package may cache it. The first array call for a signature can build a result table; later calls gather from it. Every table entry is produced by the scalar path and compared against it by the test gates, so declining the table — stochastic specs draw per element, and large signatures exceed the byte budget — means running the same scalar path per element, never computing a different answer. table_policy reports which shape a signature receives and why. When benchmarking, measure the first (table-building) call separately from warm calls.\n\nSorting uses integer order keys and, for large vectors, an O(n) counting sort. Order follows the draft's total order, with the single NaN first.","title":"Arrays and tables"},{"category":"section","location":"data.html#Choosing-a-format","page":"Data at Scale","text":"Format choice is a measurement, not a guess. At fixed bitwidth, precision trades against range, and the trade should be evaluated on data shaped like yours. For a unit-scale Gaussian sample of 50 000 values projected through each 8-bit signed extended format under RNE_SF:\n\nBinary8p5se  rmse = 0.0133   maxfinite = 15.0\nBinary8p4se  rmse = 0.0266   maxfinite = 224.0\nBinary8p3se  rmse = 0.0528   maxfinite = 49152.0\nBinary8p2se  rmse = 0.103    maxfinite = 2.147483648e9\n\nRMSE roughly doubles for each significand bit given up, while the finite range grows enormously. For this sample no format overflows, so the extra range is unused and the highest precision wins. On wider-spread data the comparison changes, which is why three numbers should accompany any quantization choice: aggregate error (RMSE), worst single error, and the overflow or clamp rate. A format can score well on the first and fail on either of the others. When the dynamic range genuinely exceeds what the precision budget allows, do not buy range with a wider element format — get it from a block scale, below.\n\nUnsigned formats suit quantities that are never negative (probabilities, memberships, normalized scores); finite formats suit domains where an infinity would be meaningless. Both spend otherwise-idle code points on resolution.\n\nThe decision, as a checklist:\n\nNever negative? Use an unsigned format (u).\nIs an infinity a meaningful outcome? If not, use a finite format (f).\nRun the three-number report (RMSE, worst error, overflow rate) on data shaped like yours; take the highest precision whose overflow rate is acceptable.\nRange still exceeds the precision budget? Use a block scale, below — not a wider element format.\nStorage- or bandwidth-bound? Pack, at the end (packing changes layout, not the quantization).","title":"Choosing a format"},{"category":"section","location":"data.html#Blocks","page":"Data at Scale","text":"When data spans more range than a narrow element format holds, widening the element spends bits on exponent that were wanted for precision. A Block{B,FS,FE} shares one scale (format FS) across B elements (format FE); each lane represents scale × element. This is the draft's MX-style scheme. Construct a block directly, or quantize a tuple and let the draft's algorithm derive the scale from the largest finite magnitude:\n\njulia> Block(Binary8p1uf(4.0), Binary8p4se(1.5), Binary8p4se(-0.75),\n             Binary8p4se(2.0), Binary8p4se(0.5))\nBlock{4, Binary8p1uf, Binary8p4se}(Binary8p1uf(4.0 ⇆ 0x82), (…))\n\njulia> ConvertToBlockMaxAbsFinite(Binary8p1uf, Binary8p4se, RNE_SN, RNE_SN,\n           (Binary8p4se(100.0), Binary8p4se(-12.0), Binary8p4se(0.5), Binary8p4se(3.0)))\nBlock{4, Binary8p1uf, Binary8p4se}(Binary8p1uf(64.0 ⇆ 0x86), (…))\n\nThe arguments read in order: scale format, element format, a projection for the scale, a projection for the elements, then the values. Here the algorithm chose scale 64 from the largest element, 100, and projected each element against it. ConvertFromBlock(T, ρ, b) reads a block back out as plain values, decoding each scale × element exactly and projecting once. Scale formats are conventionally unsigned with precision 1 (Binary8p1uf): every datum is a power of two, so dividing by the scale is exact.\n\nBlock reductions are where the exactness contract does real work. A reduction decodes every lane exactly, accumulates exactly — an integer span filter proves a Float128 accumulator suffices, or an arbitrary-precision accumulator sized from the formats takes over — and projects once:\n\njulia> b = Block(Binary8p1uf(0.5), (Binary8p4se(80.0), Binary8p4se(-0.375),\n                                    Binary8p4se(-5.5), Binary8p4se(2.25)));\n\njulia> SmallFloats.blockdecode(b)              # the exact lane values\n(40.0, -0.1875, -2.75, 1.125)\n\njulia> BlockReduceAdd(Binary8p4se, RNE_SN, b)  # exact sum 38.1875, projected once\nBinary8p4se(40.0 ⇆ 0x6a)\n\njulia> lanes = SmallFloats.blockdecode(b);\n\njulia> foldl((a, v) -> Add(Binary8p4se, RNE_SN, a, Convert(Binary8p4se, RNE_SN, v)),\n             lanes; init = zero(Binary8p4se))  # rounding at every step\nBinary8p4se(36.0 ⇆ 0x69)\n\nThe exact sum is 38.1875 and the correct projection is 40. Lane-by-lane accumulation lands on 36 — a defined-result error that grows with B. BlockDotProduct gives the same guarantee for fused products: on random 32-lane blocks, the entire difference from the Float64 dot product is the initial quantization of the inputs; accumulation contributes nothing.\n\nOne pitfall dominates block practice. ConvertToBlockMaxAbsFinite quantizes a tuple, deriving the scale from the largest finite magnitude — but it takes elements that are already Binary. If raw data is staged through the narrow element format first, SatFinite clamps the large values before the block scale can absorb them, and blocking gains nothing. Measured on a 64×64 matrix whose rows span roughly 2^16 in scale: direct Binary8p4se conversion has relative error 0.616, element-format staging 0.617, and staging through the wide-range Binary8p2se before block-quantizing 0.109 — the staging format's own precision. Stage wide, then block-quantize. At B = 32 with an 8-bit scale, storage costs 8.25 bits per value.","title":"Blocks"},{"category":"section","location":"data.html#Packed-storage","page":"Data at Scale","text":"A Vector{Binary5p2se} spends a full byte on each 5-bit code point. PackedVector(v) stores elements at true bit width. It is a complete AbstractVector{F} — indexing, iteration, collect — and vmap computes on it directly, unpacking tiles internally:\n\njulia> n = 1_000_000;\n\njulia> model = [Binary5p2se(UInt8(rand(0:31))) for _ in 1:n];\n\njulia> pv = PackedVector(model);\n\njulia> (sizeof(model), sizeof(pv.data))\n(1000000, 625000)\n\nThe rule is store packed, compute unpacked; there is deliberately no in-place packed arithmetic. Packing pays when memory or bandwidth is the constraint — for example, a large quantized model held resident. For compute-only work an ordinary Vector{F} avoids the unpack cost.","title":"Packed storage"},{"category":"section","location":"data.html#Float16-and-BFloat16","page":"Data at Scale","text":"Binary16p11se resembles Float16, and Binary16p8se resembles BFloat16, but P3109's exponent bias differs from IEEE's by one (16 versus 15; 128 versus 127). Every code point therefore denotes a value a factor of two away from its IEEE counterpart; largest finite values are 65472 versus 65504, there is one NaN rather than 2046 encodings, and no negative zero.\n\njulia> using SmallFloats.Formats\n\njulia> Convert(Binary16p11se, RNE_SF, Float16(1.5))    # a conversion: 1.5 ↦ 1.5\nBinary16p11se(1.5 ⇆ 0x4200)\n\njulia> reinterpret(Binary16p11se, Float16(1.5))        # the same bits: 1.5 ↦ 0.75\nBinary16p11se(0.75 ⇆ 0x3e00)\n\nThe reinterpreted value is wrong by a factor of two, with no error and no warning — the most dangerous operation in cross-format work, because the numbers look plausible. Use Convert across the IEEE boundary, never reinterpret. datumsexact reports which format pairs round-trip losslessly.","title":"Float16 and BFloat16"},{"category":"section","location":"data.html#Next","page":"Data at Scale","text":"A Worked Example composes this chapter and the two before it into one measured quantization task, end to end.","title":"Next"},{"category":"section","location":"assurance.html#Assurance","page":"Assurance","text":"When two runs disagree, exactly three causes are legitimate: the policy changed, the policy itself requests randomness, or a registered approximation was selected. Anything else is a defect. This chapter provides the tools that make that determination and the records that support it: the provenance test, the measured-approximation registry, the exportable conformance declaration, and a summary of how the defined path is verified.","title":"Assurance"},{"category":"section","location":"assurance.html#The-provenance-test","page":"Assurance","text":"When two runs differ, check in order:\n\nDid the operation, result format, or projection specification change?\nIs the projection stochastic, and did the draw or RNG stream change?\nDid the call explicitly select a registered approximation?\nDid process-global defaults change between calls?\n\nIf all four answers are no, a changed code point is a defect and should be reported as one. For experiments that must be reproducible, record the result format, the projection specification, the RNG algorithm and seed, and any approximation's name and measured bound alongside the results.","title":"The provenance test"},{"category":"section","location":"assurance.html#Measured-approximation","page":"Assurance","text":"The default API never substitutes an approximation. A faster, inexact kernel enters only through the registry, and the registry measures its worst-case deviation — in code points along the total order, exhaustively where the input space permits — rather than accepting a declared figure:\n\njulia> one4 = Binary8p4se(1.0);\n\njulia> hardtanh(x) = Clamp(Binary8p4se, RNE_SN, x, Negate(one4), one4);\n\njulia> κ, exhaustive = measure_kappa(hardtanh, :Tanh, Binary8p4se,\n                                     (Binary8p4se,), RNE_SN)\n(4.0, true)                # worst deviation: 4 code points, all 256 inputs checked\n\nA declaration that understates the measurement is rejected, and no partial registration state remains:\n\njulia> register_approx!(:hardtanh_act, :Tanh, Binary8p4se,\n                        (Binary8p4se,), RNE_SN, hardtanh; κ=1)\nERROR: ArgumentError: declared κ = 1.0 understates measured κ = 4.0 — registration rejected\n\nRegistered with a truthful bound, the implementation is retrieved by name (approx, list_approx), its κ is queryable, and it appears in the conformance declaration automatically. Code-point bounds also support decision rules: two defined results more than 2κ apart cannot have their comparison inverted by the approximate evaluator, which makes pruning with a cheap evaluator provably sound.","title":"Measured approximation"},{"category":"section","location":"assurance.html#The-conformance-declaration","page":"Assurance","text":"conformance() returns a live statement of the session: formats, operations, mode vocabulary, the table specializations built so far, and every registered approximation. conformance_dict() is the plain-Dict form for JSON or TOML export:\n\nd = conformance_dict()\n(d[\"package\"], length(d[\"formats\"]), length(d[\"operations\"]),\n [s[\"op\"] for s in d[\"cached_specializations\"]])\n\n(\"SmallFloats.jl 0.4.0\", 504, 52, Any[])\n\nThe first three entries are package properties: 504 formats, 52 operations, always. The fourth is session-dependent — empty here because this session had made no array call yet — so snapshot the declaration after the work, not at the top of a script. draft_revision() reports the implemented draft revision (IEEE P3109/D1, uploaded 2026-07-17); include it with any exported artifact.","title":"The conformance declaration"},{"category":"section","location":"assurance.html#Verifying-your-own-code","page":"Assurance","text":"The same exhaustive method is available to application code, because format input spaces are small enough to enumerate. A stochastic-rounding distribution, for example, is checkable exactly rather than statistically: the value 2 + 3/64 sits at fraction 3/16 of an ulp, so StochasticA{4} must round it up for exactly 3 of the 16 draws —\n\nσ4 = RSA_SN(4)\nx = 2.0 + 3/64\ncount(decode(SmallFloats.project(Binary8p4se, σ4, x; R)) == 2.25 for R in 0:15)\n\n3\n\nSweeping R turns a statistical question about bias into an exhaustive one.\n\nOrdering guarantees are checkable the same way. Ranking and decision systems consume scores through order, so a conversion between formats must be monotone — and with a few hundred datums per format, that is provable by walking every value in total order and confirming its image never goes backward:\n\nusing SmallFloats: order_key\n\nfunction monotone_conversion(::Type{From}, ::Type{To}) where {From,To}\n    prev = nothing\n    for v in sort(From.(0x00:UInt8(2^bitwidth(From) - 1)))\n        isnan(decode(v)) && continue\n        g = Convert(To, RNE_SN, v)\n        prev !== nothing && order_key(g) < order_key(prev) && return false\n        prev = g\n    end\n    true\nend\nmonotone_conversion(Binary8p3se, Binary8p4se)\n\ntrue\n\nA ranking pipeline whose conversions all pass this audit cannot invert a preference by changing formats. The same pattern — enumerate the inputs, compare against an independently computed reference — verifies quantizers and custom fused kernels; the full manual's Verify Custom Code page gives worked recipes.","title":"Verifying your own code"},{"category":"section","location":"assurance.html#How-the-defined-path-is-verified","page":"Assurance","text":"The test suite enumerates rather than samples wherever the input space allows, and reports \"sampled\" explicitly where it does not. Because formats are finite, most correctness claims are checked by exhaustion: every code point of every format round-trips; cached tables compare entry-for-entry with the scalar path; the optimized rounding core compares against the generic one; and two independent high-precision references (Float128-first and pure-MPFR builds) must agree bit-for-bit. Disabling the Float128 machinery (ENV[\"SmallFloats_Float128\"] = \"disable\") is itself a tested equivalence: identical results, different speed.\n\nStructurally, there is one write path. Every code point the package produces passes through RoundToPrecision → Saturate → Encode, fed by exact arithmetic on a carrier the format selects — Float64 where exact, Float128 above it, an exact dyadic carrier for the widest formats, with MPFR enclosures deciding the remainder. No second rounding routine exists, and nothing approximate is reachable outside the registry. The full manual's Technical Guide covers the architecture, the oracle's rigor classes, and the verification gates in detail.","title":"How the defined path is verified"},{"category":"section","location":"assurance.html#Next","page":"Assurance","text":"Quick Reference collects the names and signatures from all four chapters in brief.","title":"Next"},{"category":"section","location":"glossary.html#Glossary","page":"Glossary","text":"One line per term, with the chapter that develops it.\n\nTerm Meaning Chapter\ndatum One exact value in a format's finite set. Values\ncode point The integer naming a datum; what a value stores. Values\ncode unit The storage word for code points: UInt8 at K ≤ 8, UInt16 above. Values\nformat A finite datum set with an encoding: Binary{K,P,SGN,EXT}. Values\nalias The concrete named subtype (Binary8p4se) code should use. Values\nbinade The span between consecutive powers of two; datum spacing doubles per binade. Values\nulp The datum spacing at a given magnitude (unit in the last place). Values\nprojection The single rounding-plus-saturation step from exact result to code point. Computing\nProjSpec A named (rounding mode, saturation mode) pair, e.g. RNE_SN. Computing\nSatNone The draft's full boundary rows — not \"no saturation\". Computing\nstochastic mode Rounding whose landing depends on a random draw R; a defined distribution. Computing\nsession default One of five process-global settings behind + and friends. Computing\ndatum carrier The exact type decode returns: Float64, Float128, or dyadic. Values\npromotion carrier Where Binary + ordinary number lands: Float64, or BigFloat for wide formats. Computing\ndefined result The draft-required code point for an operation, format, and spec. Assurance\ntable A cached finite operation, entries produced by the scalar path. Data at Scale\nblock / scale B elements sharing one scale; each lane is scale × element. Data at Scale\nspan filter The integer test proving a Float128 accumulation is exact. Data at Scale\nPackedVector Storage at true bit width; compute unpacks internally. Data at Scale\nκ (kappa) A registered approximation's measured worst-case error, in code points. Assurance\ntotal order The draft's complete ordering; the single NaN sorts first. Values\nexhaustive Checked over every member of the input space, as opposed to sampled. Assurance","title":"Glossary"},{"category":"section","location":"index.html#SmallFloats.jl","page":"Overview","text":"A compact guide to the package. The full manual covers workflows, complete reference tables, and implementation detail.\n\nSmallFloats.jl implements the IEEE P3109 draft standard, Arithmetic Formats for Machine Learning. It provides all 504 legal formats at bitwidths 3 through 16, with bit-exact defined results and explicit control over rounding policy.\n\nIn formats this small, most operations round: the numbers you construct and the results you compute are usually not representable. The package makes the rounding visible and controllable rather than implicit. Three definitions carry the whole model:\n\nA format is a finite set of exact values, called datums, each named by an integer code point.\nA value stores one code point. Reading the datum back with decode is always exact.\nAn operation decodes its operands, computes the exact mathematical result, and applies one projection — a rounding choice plus a saturation choice — to produce the result code point.\n\nEach chapter of this guide develops part of that model.","title":"SmallFloats.jl"},{"category":"section","location":"index.html#Installation","page":"Overview","text":"Requires Julia 1.12 or later. The package is not yet registered; install by URL:\n\npkg> add https://github.com/JeffreySarnoff/SmallFloats.jl\n\nQuadmath (Float128) is a hard dependency. It is used only inside exact oracle and fallback paths, never anywhere it could change a result.","title":"Installation"},{"category":"section","location":"index.html#A-first-check","page":"Overview","text":"julia> using SmallFloats\n\njulia> Binary8p4se(1.6) + Binary8p4se(0.25)\nBinary8p4se(1.875 ⇆ 0x47)\n\njulia> conformance() !== nothing\ntrue\n\nThe first line exercises the projection engine: 1.6 is not representable, so construction rounds it to 1.625, and the sum lands exactly on 1.875. The second line confirms the conformance machinery is available. Values in this guide display as value ⇆ code point, showing both halves of every result.","title":"A first check"},{"category":"section","location":"index.html#What-the-package-guarantees","page":"Overview","text":"Four properties distinguish the package. They are stated plainly here and developed in the chapters that follow.\n\nNo hidden rounding. An operation computes its exact mathematical result and rounds it exactly once. There is no intermediate precision to accumulate error inside an operation, and no fast path that substitutes a different answer: the code point you receive is the one the standard defines, every time.\n\nReproducible policy. The rounding rule is not something you must remember about the session — it can be written into each call. Code that names its rule produces identical results in every session, on every machine.\n\nNo silent approximation. Faster, inexact implementations exist only if you register one, by name. Registration measures the implementation's worst-case error over every input and rejects any declared bound that understates the measurement. Nothing approximate can be reached by accident.\n\nOne behavior at every size. All 504 formats, from 3 bits to 16, follow the same rules. A wider format costs more storage; it never means something different.","title":"What the package guarantees"},{"category":"section","location":"index.html#Contents","page":"Overview","text":"Formats and Values: the format space, construction, and the code-point rule. Computing: the projection contract and the policy choices. Data at Scale: choosing a format, arrays, blocks, packed storage, and IEEE interop. A Worked Example: the chapters composed into one measured quantization task. Assurance: conformance export, measured approximation, and verification. Three lookups close the guide: Quick Reference, Troubleshooting (symptom-indexed), and a Glossary.","title":"Contents"},{"category":"section","location":"computing.html#Computing","page":"Computing","text":"Every numeric operation follows the same three steps: decode the operands, compute the mathematically exact result over the extended reals, and project once. There is no arithmetic \"in the format\" and no accumulated rounding inside an operation. Rounding error arises only at the single point where the exact result becomes a code point. This chapter covers that point: the two policy choices that decide it, and how to keep the decision stable.","title":"Computing"},{"category":"section","location":"computing.html#One-projection,-named-in-the-call","page":"Computing","text":"julia> x, y = Binary8p4se(1.6), Binary8p4se(0x46);\n\njulia> Add(Binary8p4se, RNE_SN, x, y)\nBinary8p4se(3.5 ⇆ 0x4e)\n\nThe call reads left to right: operation, result format, projection specification, operands. The exact sum, 1.625 + 1.75 = 3.375, is not a datum; it lies exactly halfway between 3.25 and 3.5, so the stored result is determined entirely by policy. RNE_SN rounds to nearest with ties to even, selecting 3.5, then applies the draft's SatNone boundary rules. A different projection could land the same sum on 3.25. The projection therefore appears in the argument list: it is part of the operation, not an ambient setting.\n\nThe ordinary Julia spelling uses the current session default:\n\njulia> x + y\nBinary8p4se(3.5 ⇆ 0x4e)\n\nUse + and the other operators for interactive work. Use Add(T, ρ, ...) in code whose results must not depend on session state.","title":"One projection, named in the call"},{"category":"section","location":"computing.html#Rounding-and-saturation","page":"Computing","text":"A ProjSpec pairs two independent choices. Rounding selects the neighbor: six deterministic modes (nearest-ties-even, nearest-ties-away, toward positive, toward negative, toward zero, to-odd), with every pairing predefined under names such as RNE_SN and RTZ_SF. Saturation decides what an out-of-range result becomes: SatFinite clamps to the finite extremes, SatPropagate preserves representable infinities, and SatNone applies the draft's full domain-, sign-, and direction-dependent rows. SatNone does not mean \"no saturation\"; it means neither simplifying policy was selected.\n\nThe axes separate cleanly on one overflow. Note first that Binary8p4se(200.0) already projected during construction — 200 ties between 192 and 208 and lands on 192 — so both multiplies below compute the exact product 384, which exceeds the largest finite datum, 224:\n\njulia> w, two = Binary8p4se(200.0), Binary8p4se(2.0);\n\njulia> Multiply(Binary8p4se, RNE_SN, w, two)   # nearest + draft rows → Inf\nBinary8p4se(Inf ⇆ 0x7f)\n\njulia> Multiply(Binary8p4se, RNE_SF, w, two)   # nearest + clamp → 224\nBinary8p4se(224.0 ⇆ 0x7e)\n\njulia> Multiply(Binary8p4se, RTZ_SN, w, two)   # toward zero + draft rows → 224\nBinary8p4se(224.0 ⇆ 0x7e)\n\nThe first two calls differ in saturation; the first and third differ in rounding. The third is the subtle case: rounding runs first and knows nothing about range, and saturation then classifies the out-of-range result with the rounding mode passed in — so a directed mode pointing away from the overflow yields the finite extreme even under SatNone. Choose SatNone when an overflow should remain observable as an infinity, SatFinite when downstream code cannot absorb one.","title":"Rounding and saturation"},{"category":"section","location":"computing.html#Stochastic-rounding","page":"Computing","text":"The three stochastic families (StochasticA/B/C, with random-bit budget N) make the landing depend on the exact fractional position and a draw R ∈ 0:2^N−1. The distribution is part of the definition: projecting 2 + 3/64 under RSA_SN(4), a value three sixteenths of the way from 2.0 to 2.25, rounds upward for exactly 3 of the 16 possible draws. Fixing R makes a single projection exactly reproducible, which suits tests:\n\njulia> σ = RSA_SN();                 # StochasticA, default N = 8\n\njulia> a, b = Binary8p4se(2.0), Binary8p4se(0.03125);\n\njulia> Add(Binary8p4se, σ, a, b; R = 0)\nBinary8p4se(2.0 ⇆ 0x48)      # smallest draw: rounds down\n\njulia> Add(Binary8p4se, σ, a, b; R = 255)\nBinary8p4se(2.25 ⇆ 0x49)     # largest draw: rounds up\n\nFor streams, pass a seeded rng; stochastic projection draws from the same generator as any value draws, so one seed reproduces a whole pipeline. An out-of-range R is an error, not a silently wrapped value.","title":"Stochastic rounding"},{"category":"section","location":"computing.html#Random-values","page":"Computing","text":"rand and randn work for every format, and both accept an explicit RNG for reproducibility:\n\njulia> rand(Xoshiro(1), Binary8p4se)\nBinary8p4se(0.0703125 ⇆ 0x21)\n\njulia> rand(Xoshiro(8), Binary8p4se, 4)\n4-element Vector{Binary8p4se}:\n Binary8p4se(0.40625 ⇆ 0x35)\n Binary8p4se(0.021484375 ⇆ 0x13)\n Binary8p4se(0.75 ⇆ 0x3c)\n Binary8p4se(0.28125 ⇆ 0x31)\n\nScalar rand takes a projection keyword. The default is a floor projection, which keeps results in [0, 1) as rand's contract requires; opting into RTP_SN or RNE_SN can return exactly 1.0, since mass near the top rounds up. The array and in-place forms always use the defaults — for an array under another projection, draw scalars explicitly.","title":"Random values"},{"category":"section","location":"computing.html#Session-defaults","page":"Computing","text":"Five process-wide defaults back the convenience forms: DefaultType, DefaultReturnType, DefaultRoundingMode, DefaultSaturationMode, and DefaultProjection. The projection default and its two components remain coherent regardless of which setter ran last:\n\nDefaultProjection() === ProjSpec(DefaultRoundingMode(), DefaultSaturationMode())\n\nA setter mutates state shared by every module in the process, and nothing scopes or restores it:\n\njulia> w * two                            # follows the default: RNE_SN → Inf\nBinary8p4se(Inf ⇆ 0x7f)\n\njulia> DefaultProjection!(RTZ_SF);\n\njulia> w * two                            # every convenience call changed\nBinary8p4se(224.0 ⇆ 0x7e)\n\njulia> DefaultProjection!(RNE_SN)         # restore; the setting is process-wide\n(NearestTiesToEven, SatNone)\n\njulia> Multiply(Binary8p4se, RNE_SN, w, two)   # explicit ρ never reads a default\nBinary8p4se(Inf ⇆ 0x7f)\n\nLibrary code and experiments should name T and ρ explicitly. The setters are appropriate for interactive sessions you control.","title":"Session defaults"},{"category":"section","location":"computing.html#Interaction-with-Julia's-numeric-model","page":"Computing","text":"Same-format expressions use ordinary Julia spellings — + - * /, exp, sqrt, comparisons — each equivalent to one draft operation under the session default. Three boundaries are deliberate:\n\nMixed Binary formats do not promote. No automatic rule can soundly pick a containing format (most format pairs have none within the grid, and pairwise selection is not associative), so cross-format arithmetic requires an explicit Convert.\nBinary with an ordinary number promotes to the format's carrier. Binary8p4se(1.5) + 2.0 returns the Float64 value 3.5, not a Binary value; the 72 wide formats promote to BigFloat instead, since a Float64 cannot hold their datums. Project back explicitly when the result should be in the format.\nSome inputs are refused rather than double-rounded. Convert accepts every type it can project exactly: Binary values, machine floats, Float128, integers, BigFloat. A Rational would require a hidden second rounding to reach a carrier, so it throws with guidance — convert to a supported carrier first, making that rounding your explicit choice.\n\nTwo draft semantics differ from IEEE 754 by design. Divide(x, 0) is NaN for every x, including ±Inf, and Recip(±Inf) is 0 — there are no signed-infinity quotients. And the total order places the single NaN first, below −Inf, so sorting a Binary vector leads with NaN.","title":"Interaction with Julia's numeric model"},{"category":"section","location":"computing.html#Next","page":"Computing","text":"This chapter computed one value at a time. The same contract must hold for large arrays, fused reductions, and storage at true bit width, without acceleration changing any code point. That is the subject of Data at Scale.","title":"Next"}]
}
